# 波动率计算文档

本文档系统介绍金融市场中常用的波动率计算方法，包括历史波动率（实现波动率）、窗口期波动率、上行/下行波动率以及收益率偏度统计特征。

## 1. 历史波动率（Historical Volatility, HV）

### 1.1 定义

历史波动率（Historical Volatility, HV），也称为实现波动率（Realized Volatility, RV），是基于历史价格数据计算的实际波动率度量，反映资产在特定期间内的真实波动水平。

### 1.2 计算公式

步骤1：计算对数收益率

$$
r_t = \ln\left(\frac{S_t}{S_{t-1}}\right)
$$

其中：

- $S_t$ = $t$ 时刻的资产价格
- $S_{t-1}$ = $t-1$ 时刻的资产价格
- $r_t$ = $t$ 时刻的对数收益率

步骤2：计算历史波动率（年化）

$$
RV = |r_t| \times \sqrt{T}
$$

其中：

- $T$ = 一年交易日数量（默认 $242$）
- $r_t$ = 第 $t$ 期的对数收益率
- $RV$ = 年化历史波动率（Realized Volatility）

## 2. 窗口期波动率（Rolling Window Volatility）

### 2.1 定义

窗口期波动率是在特定时间窗口内计算的波动率，用于观察波动率随时间的变化趋势。

### 2.2 计算公式

$$
\sigma_w = \sqrt{\frac{1}{w} \cdot \sum_{i=0}^{w-1} r_{t-i}^2}
$$

其中：

- $w$ = 窗口期长度
- $r_{t-i}$ = 窗口内第 $i$ 期的对数收益率

### 2.3 年化处理

$$
\sigma_{w,\text{annual}} = \sigma_w \times \sqrt{T}
$$

其中：

- $T$ = 一年交易日数量（默认 $242$）

### 2.4 常用窗口期含义

| 窗口期 | 含义 |
| ------ | ---- |
| $w = 5$ | 周波动率 |
| $w = 10$ | 双周波动率 |
| $w = 20$ | 月波动率（约1个月交易日） |
| $w = 60$ | 季度波动率 |
| $w = 120$ | 半年波动率 |

## 3. 上行波动率与下行波动率（Up Volatility & Down Volatility）

### 3.1 概念

传统波动率将上涨和下跌同等对待，但市场参与者往往更关注下行风险。上行/下行波动率将收益率按正负分离计算，区分“好波动”与“坏波动”。

### 3.2 上行波动率（Up Volatility）

仅考虑正收益（价格上涨）时的波动，不减均值：

$$
\sigma_{\text{up}} = \sqrt{\frac{T}{n_{up}} \cdot \sum_{t=1}^{N} r_t^2 \cdot \mathbb{I}(r_t > 0)}
$$

其中：

- $T$ = 一年交易日数量（默认 $242$）
- $N$ = 样本观测数
- $n_{up}$ = 正收益观测数量
- $\mathbb{I}(r_t > 0)$ = 指示函数，当 $r_t > 0$ 时为 $1$，否则为 $0$
- $r_t$ = 第 $t$ 期对数收益率

### 3.3 下行波动率（Down Volatility）

仅考虑负收益（价格下跌）时的波动，不减均值：

$$
\sigma_{\text{down}} = \sqrt{\frac{T}{n_{down}} \cdot \sum_{t=1}^{N} r_t^2 \cdot \mathbb{I}(r_t < 0)}
$$

其中：

- $T$ = 一年交易日数量（默认 $242$）
- $N$ = 样本观测数
- $n_{down}$ = 负收益观测数量
- $\mathbb{I}(r_t < 0)$ = 指示函数，当 $r_t < 0$ 时为 $1$，否则为 $0$
- $r_t$ = 第 $t$ 期对数收益率

## 4. 偏度（Skewness）

### 4.1 定义

偏度衡量收益率分布的不对称性，反映极端收益方向和尾部风险特征。在风险中性或收益均值可忽略的场景下，可用原始三阶矩来衡量偏度。

### 4.2 计算公式

样本偏度：

$$
S = \frac{\frac{1}{N} \sum_{t=1}^{N} r_t^3}{\left(\frac{1}{N} \sum_{t=1}^{N} r_t^2\right)^{3/2}}
$$

其中：

- $N$ = 观测数
- $r_t$ = 第 $t$ 期收益率

## 5. 总结

| 指标 | 用途 | 特点 |
| ---- | ---- | ---- |
| 历史波动率 HV | 整体风险度量 | 简单直观，反映已实现波动 |
| 窗口期波动率 | 动态风险监控 | 捕捉波动率时变特征 |
| 上行/下行波动率 | 方向性风险分析 | 区分好坏波动，关注下行保护，不减均值 |
| 偏度 | 分布不对称性 | 识别极端风险方向 |

## 附录：符号说明

| 符号 | 含义 |
| ---- | ---- |
| $S_t$ | $t$ 时刻资产价格 |
| $r_t$ | 对数收益率 |
| $T$ | 一年交易日数量，年化基数（默认 $242$） |
| $w$ | 窗口期长度 |
| $n_{up}$ | 正收益观测数量 |
| $n_{down}$ | 负收益观测数量 |
| $\bar{r}$ | 样本平均收益率 |
| $\sigma$ | 标准差 / 波动率 |
| $\mathbb{I}(\cdot)$ | 指示函数 |

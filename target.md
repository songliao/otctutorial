# 重叠滚动窗口波动率\&不重叠隔日波动率 统一年化波动率计算公式（无中间日度变量）

## 1\. 前置统一符号定义（全文唯一符号，无冲突）

|符号|变量含义|
|---|---|
|$P_t$|第$t$个交易日标的资产收盘价，$t=1,2,3...N$，$N$为总交易日数量|
|$r_t$|单交易日对数收益率，$r_t = \ln \frac{P_t}{P_{t-1}}$|
|$w$|波动率计算窗口长度（交易日），隔日波动率固定取 $w=3$|
|$s$|窗口滑动步长（交易日），核心区分参数，控制窗口是否重叠|
|$\Delta t$|单交易日年化时间间隔，国内市场统一取值：$\Delta t = \frac{1}{242}$|
|$\sigma_k$|第$k$个窗口对应的**年化波动率**（全文直接输出该指标，无日度波动率中间值）|
|$k$|窗口序号，$k=1,2,3...K$，$K$为可计算的总窗口数量|

---

## 2\. 通用一体化年化波动率公式（兼容两种场景，一步计算）

假设第$k$个波动率窗口的起始交易日为 $T_k = s \cdot (k-1) + 1$，窗口覆盖区间：$[T_k,\ T_k+w-1]$。

全程跳过日度波动率，**直接一步求解年化波动率**，统一通用公式：

$\sigma_k = \sqrt{ \frac{1}{w \cdot \Delta t} \sum_{t=T_k}^{T_k+w-1} r_t^2 }$

**核心说明**：两种波动率本质为同一公式，仅修改**窗口步长s**即可切换，隔日波动率是该通用公式下的特殊不重叠窗口情形，二者底层计算逻辑完全一致。

---

## 3\. 场景一分化：重叠滚动窗口波动率（日常滚动测算）

### 参数配置

- 窗口长度：自定义$w$

- 滑动步长：$s=1$（每日滑动1个交易日，窗口高度重叠，每日生成1个波动率数据）

### 代入后专属公式

窗口起始点：$T_k = k$，公式简化为：

$\sigma_k = \sqrt{ \frac{1}{w \cdot \Delta t} \sum_{t=k}^{k+w-1} r_t^2 }$

**输出结果**：每日更新一条年化波动率序列，数据连续性强，适合追踪波动率逐日变化。

---

## 4\. 场景二分化：3日不重叠隔日波动率（特殊限定场景）

### 参数配置

- 窗口长度：固定 $w=3$

- 滑动步长：$s=w=3$（窗口无任何重叠，每滑动3个交易日才生成1条波动率数据）

### 代入后专属公式

窗口起始点：$T_k = 3(k-1)+1$，公式简化为：

$\sigma_k = \sqrt{ \frac{1}{3 \cdot \Delta t} \sum_{t=3(k-1)+1}^{3k} r_t^2 }$

**输出结果**：每3个交易日输出一条年化波动率，窗口完全独立无数据重叠，规避重叠窗口带来的序列自相关问题。

---

## 5\. 统一逻辑总结

1. 两种波动率模型共用**同一条核心年化波动率公式**，无两套独立算式，符号完全统一，不存在变量歧义；

2. 仅通过调整步长参数$s$区分重叠/非重叠窗口，隔日波动率为通用公式的子集特例；

3. 全程剔除日度波动率中间计算环节，所有指标直接输出最终需要的**年化波动率**，公式更简洁、报告行文更严谨。

> （注：文档部分内容可能由 AI 生成）
